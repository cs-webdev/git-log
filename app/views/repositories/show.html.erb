<h1> Commits of <%= @repo.working_dir %></h1>
<ul>
	<% @logs.each do | log |  %>

	<li><%= link_to log.message, repository_commit_path(@repo,log.sha) %></li>
	<% end %>
</ul>

<ul>
  <% @branches.each do | branch |  %>

  <li><%= branch.full %></li>
  <% end %>
</ul>
